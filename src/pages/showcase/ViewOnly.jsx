// ============================================================
// HYELEARNER: FOUNDATION — VIEW ONLY
// Free demo: preview is always on. Renders children as-is.
// Kept as a passthrough so showcase files don't need editing.
// Built by Hyesent.dev
// ============================================================

export function ViewOnly({ children }) {
  return children
}
